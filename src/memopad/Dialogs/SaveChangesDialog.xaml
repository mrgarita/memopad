<Window x:Class="Memopad.Dialogs.SaveChangesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="memopad"
        Width="420"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <!-- メモ帳と同じ「保存／保存しない／キャンセル」の 3 択にする（MessageBox だと「はい／いいえ」になってしまう） -->
    <StackPanel Margin="20">
        <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="14" Margin="0,0,0,20" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveButton" Content="保存(_S)" MinWidth="96" Margin="0,0,8,0" IsDefault="True" Click="SaveButton_Click" />
            <Button x:Name="DontSaveButton" Content="保存しない(_N)" MinWidth="96" Margin="0,0,8,0" Click="DontSaveButton_Click" />
            <Button x:Name="CancelButton" Content="キャンセル" MinWidth="96" IsCancel="True" />
        </StackPanel>
    </StackPanel>
</Window>
